services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app/frontend
      - ./backend:/app/backend
      - db_data:/app/backend/data
    environment:
      - PORT=3000

volumes:
  db_data:
